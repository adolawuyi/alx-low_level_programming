#include "main.h"
/**
 * _abs - compute the absolute value of an integer
 *@n: Integer to be computed.
 *
 * Retuurn: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
	return (n);

	else
	return (-n);
}
