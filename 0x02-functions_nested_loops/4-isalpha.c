#include "main.h"
/**
 *_isalpha - returns 1 if c is a letter, lowercase or uppercase,
 otherwise return 0.
 *@c: takes in a character
 *Return: 1 if c is a letter, 1 for otherwise
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || c >= 'a' && c <= 'z');
	return (1);

	else
	return (0);
}
